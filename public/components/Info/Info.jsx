
const React = require( 'react' );

module.exports = React.createClass({

    render() {

        return(
            <div id="info">
                <h2>info</h2>
            </div>
        );
    }
});
